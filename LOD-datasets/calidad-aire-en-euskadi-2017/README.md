url: http://opendata.euskadi.eus/catalogo/-/calidad-aire-en-euskadi-2017/

Los recursos deben tener asociado un label y un comentario para usar la funcionalidad de autocompletado. Además todos los nuevos predicados y prefix que se utilizen posteriormente deben ser almacenados en la triplestore siguiendo el formato usado para las demostraciones para que se puedan usar en las distintas funcionalidades de autocompletado y adición de prefix.

Para el csv calidad del aire se realizarán gráficos para representar los distintos valores que tomarán las mediciones, además para poder utilizar la geo localización de la estación donde se realizaron las mediciones, se pasó a RDF el csv de estaciones para poder tomar de alli los datos referentes a la geo localizacion de la estación.

Para el csv de retribuciones nominativas se hace un grafico comparando los salarios por departamento gerente.

Para el csv de relaciones puestos de trabajo se hace una comparativa de precios por el puesto.

Para el csv de registro de contratos se hace una comparativa
